# Missing Assets

## Current missing asset picture
- 66 planned image slots exist across the six-product system.
- No generated or approved production images are currently stored in the visual-system product folders.
- The structural prompt gap is no longer the main blocker because the shared prompt modules now exist.
- The remaining gap is execution: generating, selecting, validating, and publishing files per slot.

## High-priority first-wave assets
- all `hero-main` assets
- all `installed-context` assets
- all `fingerprint-use` assets where applicable
- all `app-add-device` assets
- all `link-qr` assets only where the real product flow supports QR pairing

## Review-sensitive assets
- `pin-use` for products whose references do not show a conventional keypad slab
- `language-settings` for products where on-device UI evidence is weak
- any troubleshooting scene that risks inventing app errors or false screen text

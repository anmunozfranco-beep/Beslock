# Pointer-only maintenance

Runtime maintenance summaries aggregate pointer-only counts of records across all prior runtime trees (Phase 47–58). They NEVER duplicate payloads. They are NOT dashboards. They never poll, never stream, never broadcast.

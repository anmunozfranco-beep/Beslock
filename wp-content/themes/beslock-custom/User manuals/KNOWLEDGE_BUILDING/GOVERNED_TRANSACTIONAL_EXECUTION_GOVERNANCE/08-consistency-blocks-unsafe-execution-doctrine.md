# Consistency Blocks Unsafe Execution

*Layer 40. Subordinate to all 39 prior layers.*

The consistency engine detects duplicate / concurrent / orphan / partial / drift conditions and blocks unsafe execution while preserving evidence. It never auto-resolves.

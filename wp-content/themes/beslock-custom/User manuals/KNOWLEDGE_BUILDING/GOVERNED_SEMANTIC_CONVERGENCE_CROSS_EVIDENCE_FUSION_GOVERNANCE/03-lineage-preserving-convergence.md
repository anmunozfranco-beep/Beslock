# Lineage-preserving convergence

Every convergence step MUST preserve evidence_refs, grounding_refs, fingerprint_ids, and reviewer attribution. Convergence reveals one canonical surface; it never erases provenance.

# Deterministic fingerprints — no embeddings

Convergence MUST use deterministic SHA-256 fingerprints over normalized text + structural features. Embeddings, vector databases, similarity scores, and probabilistic inference are FORBIDDEN.

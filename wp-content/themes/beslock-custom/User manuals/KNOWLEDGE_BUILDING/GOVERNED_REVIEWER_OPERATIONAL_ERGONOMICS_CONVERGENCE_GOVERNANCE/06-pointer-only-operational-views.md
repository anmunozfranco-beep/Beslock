# Pointer-only operational views

Diagnostics, confidence summaries, navigation indexes, queues and handoff records are ALL pointer-only. They reference existing append-only artifacts; they NEVER copy, embed, mirror, or duplicate upstream payloads. They are NOT dashboards.

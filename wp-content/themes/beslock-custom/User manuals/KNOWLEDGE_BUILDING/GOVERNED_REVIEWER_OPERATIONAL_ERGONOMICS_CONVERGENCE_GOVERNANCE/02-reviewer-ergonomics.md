# Reviewer ergonomics

Reviewers MUST be able to navigate, onboard, audit, deploy, hand off, and recover operations through deterministic pointer-only artifacts. Guided workflows and onboarding sequences are step lists pointing at existing dispatches and runtime paths — they are NOT executors and they NEVER mutate upstream state.

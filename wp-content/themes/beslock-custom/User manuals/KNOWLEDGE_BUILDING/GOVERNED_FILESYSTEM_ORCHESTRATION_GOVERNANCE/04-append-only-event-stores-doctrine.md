# Append-Only Event Stores

*Layer 39. Subordinate to all 38 prior layers.*

All eight runtime-manifest event stores are append-only. Prior events are never modified. Rollback emits new events; it never deletes.

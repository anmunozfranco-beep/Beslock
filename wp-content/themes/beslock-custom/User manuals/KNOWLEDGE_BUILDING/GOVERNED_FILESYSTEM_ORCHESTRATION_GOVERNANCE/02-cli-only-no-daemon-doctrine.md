# CLI Only, No Daemon

*Layer 39. Subordinate to all 38 prior layers.*

There is no watcher, no scheduled trigger, no background process, no queue worker. Every mutation is a foreground reviewer-invoked command.

# 07-replay-determinism

Replay reconstructs semantic structure, package hashes, continuity chains, and lineage references end-to-end. Non-determinism and hash mismatch are fail-closed. Replay NEVER mutates prior-layer trees, the live publication tree, frontend/theme systems, or source evidence.

# 04-deterministic-package-hashing

package_sha256 is computed deterministically over canonical JSON of the semantic structure and lineage references. Hash mismatch on replay is fail-closed. Hidden semantic mutation is fail-closed.

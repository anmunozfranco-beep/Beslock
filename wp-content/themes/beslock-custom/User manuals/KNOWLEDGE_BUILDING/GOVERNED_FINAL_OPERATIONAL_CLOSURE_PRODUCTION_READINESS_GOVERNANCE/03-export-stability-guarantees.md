# Export stability guarantees

Identical semantic states MUST produce identical export payloads across rebuilds. Section ordering, canonical IDs, procedure ordering, warning grouping, troubleshooting ordering, and specification ordering MUST all be deterministic.

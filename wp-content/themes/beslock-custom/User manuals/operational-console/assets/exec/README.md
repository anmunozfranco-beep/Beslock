# assets/exec

Plain ES JavaScript modules used by the layer-38 executable consoles. No bundler, no framework, no network calls, no telemetry. localStorage is used for session and append-only drafts; SVG is used for visualisations.

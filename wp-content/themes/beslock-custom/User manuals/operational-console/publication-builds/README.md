# publication-builds/

Deterministic publication outputs (HTML + JSON) written by `tools/governed_knowledge_synthesis_executor.py --kind publication-build --confirm`.
Never overlap with the live publication tree. Each build sits under a unique build_id.

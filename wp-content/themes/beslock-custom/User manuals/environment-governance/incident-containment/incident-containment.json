{
  "containment_invariants": [
    "no incident is ever silently absorbed; every incident emits an event",
    "no incident response writes to canonical knowledge-core JSON",
    "no incident response bypasses the supervision-receipt",
    "every containment action is reversible by an equal-or-higher governance decision",
    "every incident closure is accompanied by a replay-validated finding"
  ],
  "incident_containment_patterns": [
    {
      "containment": "halt slice → quarantine sandbox → emit incident-trace event",
      "id": "runtime-incident-isolation",
      "trigger": "safety predicate failure or replay drift > floor"
    },
    {
      "containment": "auto-route to escalation-supervisor; raise SLA tier; suspend dependent slices",
      "id": "escalation-containment",
      "trigger": "escalation-trace event with no responder"
    },
    {
      "containment": "halt rollback; require governance + auditor co-sign before fan-out",
      "id": "rollback-containment",
      "trigger": "operator-initiated rollback exceeding declared scope"
    },
    {
      "containment": "demote affected node tier to candidate; halt emissions citing the node; escalation-supervisor notified",
      "id": "provenance-breach-handling",
      "trigger": "provenance hash mismatch or missing source binding"
    },
    {
      "containment": "shut down emission path in env; preserve append-only logs; quarantine env",
      "id": "unsafe-runtime-shutdown",
      "trigger": "unrecoverable safety failure"
    },
    {
      "containment": "replay frozen orchestration-trace; compare to live; emit replay-trace; auditor finding",
      "id": "replay-assisted-incident-review",
      "trigger": "any incident closed"
    }
  ],
  "kind": "incident-containment",
  "schema": "environment-integration-governance/1.0"
}

# Change detection

Declarative signals only. Detection NEVER triggers a refresh automatically -- it emits a change-detection-report consumed by the refresh-orchestrator under reviewer authorization.

# Orchestration contracts

These files declare the contracts of future orchestration components. They are NOT executable. Production CLI / runtime execution is deferred.

Contracts: refresh-orchestrator, propagation-engine, stale-detector, dependency-resolver, rebuild-manifest-generator.
